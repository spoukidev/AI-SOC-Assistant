# Research Methodology

The platform will compare reproducible classifiers using fixed random seeds and documented train/validation/test splits. Accuracy alone is insufficient: precision, recall, F1, ROC-AUC, PR-AUC, confusion-matrix counts, and timing will be stored when applicable. No result is displayed before an experiment runs.
